name: Bootstrap source registry v2

on:
  push:
    branches: [fix/source-registry-bootstrap]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: bootstrap-source-registry-v2
  cancel-in-progress: true

jobs:
  bootstrap:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/source-registry-bootstrap
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install dependencies and create deterministic lockfile
        run: npm install --no-audit --no-fund

      - name: Resolve papers, authors, organisations, implementations and datasets
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REFRESH_INSPECT: "1"
        run: npm run refresh:sources

      - name: Run structural, source-resolution, construct and search quality gates
        run: npm run check

      - name: Remove one-time bootstrap workflow
        run: rm .github/workflows/bootstrap-source-registry.yml

      - name: Commit generated source registry and lockfile
        run: |
          git config user.name "fronteraeval-bot"
          git config user.email "fronteraeval-bot@users.noreply.github.com"
          git add -A package-lock.json data/source-registry.json .github/workflows/bootstrap-source-registry.yml
          git commit -m "data: resolve source papers, authors and organisations"
          git push origin HEAD:fix/source-registry-bootstrap
